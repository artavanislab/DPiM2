enList <- read.table("/home/glocke/DPiM/dpim3.1/mcode1_10-12-2015/all.enrich.list")
flList <- read.table("/home/glocke/DPiM/dpim3.1/mcode1_10-12-2015/all.flagNet.list")

for (i in 1:nrow(enList)) {
    en <- read.table(enList
}
