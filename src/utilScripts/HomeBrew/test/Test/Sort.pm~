package Test::Sort;

use strict;
use warnings;

require Exporter;
our @ISA = qw(Exporter);
our @EXPORT = qw(testSort);
our $VERSION = '0.01';

sub testSort {
    my ($sortFunction, @data) = @_;
    return sort $sortFunction @data;
}

"no text below this line"
